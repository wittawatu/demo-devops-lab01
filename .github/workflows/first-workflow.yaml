name: first workflow
on:
  push:
    branches:
        - "main"
        - "develop"
        - "release"
jobs:
  lab-job:
      runs-on: ubunut-latest
      steps:
          - name: Start Jobs
            run: echo 'Hello DevOps'
